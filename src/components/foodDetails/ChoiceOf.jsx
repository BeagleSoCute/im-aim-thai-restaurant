import styled from "styled-components";
import { Row, Col } from "antd";

const ChoiceOf = () => {
  return <StyledRow className="choice-of">
    <Col span={24}>
    </Col>
  </StyledRow>;
};
const StyledRow = styled(Row)`
  &.choice-of {
  }
`;

export default ChoiceOf;
